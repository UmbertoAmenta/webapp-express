const connection = require("../data/db");

// INDEX
const index = (req, res) => {};

// SHOW
const show = (req, res) => {};

// DELETE
const destroy = (req, res) => {};

module.exports = { index, show, destroy };
